<h1 class="text-center"><%= @user.username %> <mark style="font-size:12px"><%= @user.created_at.strftime("medlem siden %d/%m/%Y") %></mark></h1>
<% if not @books.empty? %>
  <%= render partial: 'books/list', locals: { books: @books } %>
<% end %>
